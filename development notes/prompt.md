What chatgtp says:

> For the current “inter-species only” restriction, what is already implemented is:
> 
> What is not implemented yet is the host-system restriction or penalty from C0. The helper _gamma_prime_at() already exists in genes.py (line 398), but it is not yet used inside _sample_recipient().

---

1. `dated_gene_tree_auxiliary` vs `create_auxiliary_tree`

---

Take context from `development notes/General context.md`, no other action required. Then go to `development notes/implementation.md`  and implement the task at lines 

